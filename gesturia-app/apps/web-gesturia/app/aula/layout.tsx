export const metadata = {
  title: "Gestaula — a classroom where every child is fluent",
  description:
    "The teacher writes the lesson. Lea signs it at the front of the class. Gestaula — deaf education, taught by anyone.",
};

export default function AulaLayout({ children }: { children: React.ReactNode }) {
  return children;
}
